apiVersion: v1
kind: Service
metadata:
  name: ledger-api
  namespace: payments

spec:
  selector:
    app: ledger-api

  ports:
    - name: http
      port: 80
      targetPort: 8080

  type: ClusterIP
